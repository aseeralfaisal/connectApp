import { StyleSheet } from 'react-native'

export default styles = StyleSheet.create({
  shadow: {
    shadowColor: '#000',
    shadowOffset: {
      width: 50,
      height: 50
    },
    shadowOpacity: 1,
    shadowRadius: 400,
    elevation: 50
  },
})
